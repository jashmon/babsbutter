---
interface Props {
  image: string;
  alt: string;
  title: string;
  meta: string;
}
const { image, alt, title, meta } = Astro.props;
---
<article class="r-card">
  <img src={image} alt={alt} />
  <div class="cap"><h3>{title}</h3><span>{meta}</span></div>
</article>
